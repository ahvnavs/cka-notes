apiVersion: v1
kind: ReplicationController
metadata:
  name: myapp-rc
  labels:
    app: myapp
    type: proxy
spec:
  template:
    metadata:
      name: nginx-pod
      labels:
        app: nginx
        type: proxy
    spec:
      containers:
        - name: nginx-container
          image: nginx
  replicas: 2
